apiVersion: v1
kind: ConfigMap
metadata:
  name: gorgon-config
  namespace: gorgon
  labels:
    app.kubernetes.io/name: gorgon
data:
  GORGON_ENV: "production"
  LOG_LEVEL: "INFO"
  METRICS_ENABLED: "true"
  AUDIT_LOGGING: "true"
